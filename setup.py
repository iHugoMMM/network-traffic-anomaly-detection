from setuptools import setup, find_packages

setup(
    name='network_traffic_anomaly_detection',
    version='0.1.0',
    packages=find_packages(),  # Automatically find 'src' if it has __init__.py
)
